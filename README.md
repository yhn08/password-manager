# Password Manager

CLI password manager - learning Python file I/O and JSON

## Features
- Add/retrieve/delete passwords
- Persistent JSON storage
- Error handling

## Setup
The program creates `passwords.json` automatically on first use. This file stores your passwords locally and is excluded from git via `.gitignore`.

## Usage
```bash
python code.py
```

## Tech
Python 3.11 | JSON
